# HydraPurr — notes

## Delta

`delta` negative → voltage dropped → sensor moved further from water → water level fell → water was consumed.

## Extent

`extent` is always positive — just the total voltage swing, direction-agnostic.